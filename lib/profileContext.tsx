 "use client"; import { createContext, useContext } from "react"; export type Profile = { id: string; full_name: string; role: "gestor" | "administrador" | "vendedor" }; export function isAdminRole(role: Profile["role"]) { return role === "gestor" || role === "administrador"; } export const ProfileContext = createContext<Profile | null>(null); export function useProfile() { const ctx = useContext(ProfileContext); if (!ctx) throw new Error("useProfile precisa estar dentro do AppLayout"); return ctx; }
