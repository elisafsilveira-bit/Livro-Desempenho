https://github.com/elisafsilveira-bit/Livro-Desempenho/edit/main/app/(app)/entry/page.tsx Mesma ideia: troque import { useProfile } from "@/lib/profileContext"; por import { useProfile, isAdminRole } from "@/lib/profileContext"; Adicione logo abaixo de const profile = useProfile();: const isAdmin = isAdminRole(profile.role); Troque as duas ocorrências de profile.role === "gestor" por isAdmin, e a ocorrência de profile.role !== "gestor" por !isAdmin.
