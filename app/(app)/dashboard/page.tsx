https://github.com/elisafsilveira-bit/Livro-Desempenho/edit/main/app/(app)/dashboard/page.tsx Procure a linha: import { useProfile } from "@/lib/profileContext"; e troque por: import { useProfile, isAdminRole } from "@/lib/profileContext"; Depois procure a linha: const profile = useProfile(); e logo abaixo dela adicione uma linha nova: const isAdmin = isAdminRole(profile.role); Depois ache a linha: if (profile.role === "gestor") { e troque só essa por: if (isAdmin) { Por fim ache a linha: if (profile.role !== "gestor") { e troque só essa por: if (!isAdmin) {
