https://github.com/elisafsilveira-bit/Livro-Desempenho/edit/main/app/(app)/layout.tsx Apague tudo e cole: "use client"; import { useEffect, useState } from "react"; import { usePathname, useRouter } from "next/navigation"; import Link from "next/link"; import { supabaseBrowser } from "@/lib/supabaseClient"; import { ProfileContext, isAdminRole, type Profile } from "@/lib/profileContext"; export default function AppLayout({ children }: { children: React.ReactNode }) { const [profile, setProfile] = useState<Profile | null>(null); const [loading, setLoading] = useState(true); const router = useRouter(); const pathname = usePathname(); const supabase = supabaseBrowser(); useEffect(() => { (async () => { const { data: { user } } = await supabase.auth.getUser(); if (!user) { router.push("/login"); return; } const { data } = await supabase.from("profiles").select("*").eq("id", user.id).single(); setProfile(data as Profile); setLoading(false); })(); }, []); async function signOut() { await supabase.auth.signOut(); router.push("/login"); } if (loading || !profile) { return ( <div className="wrap"> <div className="empty-note">Carregando…</div> </div> ); } const isAdmin = isAdminRole(profile.role); const tabs = isAdmin ? [ { href: "/dashboard", label: "Painel da equipe" }, { href: "/entry", label: "Lançar atividades" }, { href: "/settings", label: "Configurações" }, ] : [ { href: "/dashboard", label: "Meu desempenho" }, { href: "/entry", label: "Lançar atividades" }, ]; return ( <div className="wrap"> <div className="masthead"> <div className="eyebrow">Livro de Desempenho &middot; Equipe Consórcio</div> <h1>Rotina Semanal</h1> <div className="sub"> {profile.full_name} · {profile.role} </div> <div className="month"> <button className="signout" onClick={signOut}> Sair </button> </div> </div> <div className="tabs"> {tabs.map((t) => ( <Link key={t.href} href={t.href} className={`tab ${pathname === t.href ? "active" : ""}`}> {t.label} </Link> ))} </div> <ProfileContext.Provider value={profile}>{children}</ProfileContext.Provider> </div> ); }
